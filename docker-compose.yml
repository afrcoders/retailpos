version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: retailpos_app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - .:/var/www/html
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    networks:
      - retailpos
    depends_on:
      - mysql
      - redis

  nginx:
    image: nginx:alpine
    container_name: retailpos_nginx
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - retailpos
    depends_on:
      - app

  mysql:
    image: mysql:8.0
    container_name: retailpos_mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-retailpos}
      MYSQL_USER: ${DB_USERNAME:-retailpos}
      MYSQL_PASSWORD: ${DB_PASSWORD:-secret}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootsecret}
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - retailpos

  redis:
    image: redis:7-alpine
    container_name: retailpos_redis
    restart: unless-stopped
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    networks:
      - retailpos

  queue:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: retailpos_queue
    restart: unless-stopped
    working_dir: /var/www/html
    command: php artisan queue:work redis --sleep=3 --tries=3 --max-time=3600
    volumes:
      - .:/var/www/html
    networks:
      - retailpos
    depends_on:
      - app
      - redis

  mailpit:
    image: axllent/mailpit:latest
    container_name: retailpos_mailpit
    restart: unless-stopped
    ports:
      - "8025:8025"
      - "1025:1025"
    networks:
      - retailpos

networks:
  retailpos:
    driver: bridge

volumes:
  mysql_data:
  redis_data:
